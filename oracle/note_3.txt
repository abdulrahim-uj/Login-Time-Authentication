SELECT * FROM TBL_GSA_ID_GENERATORE

SELECT * FROM TBL_GSA_ID_GENERATORE
WHERE TABLE_ID = (SELECT MAX(TABLE_ID) FROM TBL_GSA_ID_GENERATORE)

SELECT SUBSTR(GSA_ID,-4) FROM TBL_GSA_ID_GENERATORE
WHERE TABLE_ID = (SELECT MAX(TABLE_ID) FROM TBL_GSA_ID_GENERATORE)

SELECT LPAD(GSA_ID,6) FROM TBL_GSA_ID_GENERATORE
WHERE TABLE_ID = (SELECT MAX(TABLE_ID) FROM TBL_GSA_ID_GENERATORE)

SELECT GSA_ID FROM TBL_SIGNUP
WHERE TABLE_ID = (SELECT MAX(TABLE_ID) FROM TBL_SIGNUP)
